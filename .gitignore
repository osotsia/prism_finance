*.so
plan/
# =============================================================================
# === General: Operating System Files =========================================
# =============================================================================
# Ignore OS-specific files that clutter repositories.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
__MACOSX

# Windows
Thumbs.db
ehthumbs.db
desktop.ini


# =============================================================================
# === Python: Virtual Environments & Caches ===================================
# =============================================================================
# Ignore virtual environments, caches, and build artifacts from the Python ecosystem.
# Never commit your virtual environment.

# Virtual Environments
/venv/
/.venv/
/env/
/.env/
/env.bak/
/venv.bak/

# Python Caches
/__pycache__/
*.pyc
*.pyo
*.pyd

# IPython/Jupyter Notebook Checkpoints
.ipynb_checkpoints/

# Environment variables - never commit secrets!
.env
.env.*
!.env.example


# =============================================================================
# === Python: Testing & Packaging Artifacts ===================================
# =============================================================================
# Ignore files generated by testing, coverage, and packaging tools like maturin.

# Testing (pytest)
.pytest_cache/
.tox/

# Coverage
.coverage
.coverage.*
htmlcov/
*.cov

# Packaging (setuptools, wheel, maturin)
/build/
/dist/
/*.egg-info/
/.eggs/
# The compiled Rust core module, placed in the source tree during `maturin develop`.
# This is a build artifact, not source code.
/python/prism/_core.*


# =============================================================================
# === Rust: Build Artifacts ===================================================
# =============================================================================
# Ignore all Rust build artifacts, which are managed by Cargo.

# The target directory contains all compiled dependencies, objects, and the final binary.
# It is the single most important entry for any Rust project.
/rust/target/

# Cargo.lock is typically committed for applications to ensure reproducible builds,
# but for libraries like Prism, it is best practice to ignore it. This allows
# downstream users to use the latest compatible versions of dependencies.
# /rust/Cargo.lock


# =============================================================================
# === IDEs and Editors ========================================================
# =============================================================================
# Ignore user-specific configuration files from common editors.

# VSCode
.vscode/
# We can make an exception for shared settings if the team agrees.
# !.vscode/settings.json

# JetBrains (PyCharm, CLion, RustRover)
.idea/

# Vim/Neovim
*.swp
*.swo
*~

# Rust Analyzer cache
/.rust-analyzer-cache/


# =============================================================================
# === Documentation ===========================================================
# =============================================================================
# Ignore build outputs from documentation generators like Sphinx or MkDocs.

# Example for Sphinx
/docs/_build/

# Example for MkDocs
/site/